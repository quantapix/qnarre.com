{
  "name": "qnarre.com",
  "author": "quantapix <quantapix@gmail.com>",
  "license": "MIT",
  "main": "index.js",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prep": "git submodule update --init --recursive",
    "dev": "hugo server --config github.yml --minify",
    "build": "rm -rf public; HUGO_ENV=\"production\" hugo --config github.yml --minify --gc",
    "qnarre.com": "rm -rf public; HUGO_ENV=\"production\" hugo --config qnarre.com.yml --minify --gc; hugo deploy --config qnarre.com.yml --verbose"
  },
  "devDependencies": {
    "autoprefixer": "",
    "postcss-cli": "",
    "postcss": "",
    "prettier-plugin-go-template": "",
    "stylelint-config-twbs-bootstrap": "",
    "stylelint": ""
  }
}
